[package]
name = "flash-algo-build"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lib]
name = "flash_algo_build"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
